{
  "name": "weekmenu-app",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone Weekmenu app (own backend, own Anthropic API key)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
